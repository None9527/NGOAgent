# NGOAgent 文件说明

> Go · DDD · 55 文件 · 7,998 行 · 可编译二进制

## 目录结构

```
NGOAgent/
├── cmd/ngoagent/main.go                     # 入口 + 优雅关停
├── go.mod                                   # github.com/ngoclaw/ngoagent
├── docs/
│   ├── design.md                            # 设计文档
│   └── architecture.md                      # 架构概述
│
├── internal/
│   ├── domain/
│   │   ├── entity/                          # 领域实体
│   │   │   ├── session.go                   # Session
│   │   │   ├── message.go                   # Message
│   │   │   ├── skill.go                     # Skill + ForgeRun
│   │   │   └── knowledge.go                 # KnowledgeItem
│   │   └── service/                         # 领域服务
│   │       ├── loop.go                      # AgentLoop + Deps (12 依赖)
│   │       ├── run.go                       # ReAct 循环 + 全量提示词组装
│   │       ├── state.go                     # 10 态状态机
│   │       ├── guard.go                     # BehaviorGuard 4 规则
│   │       ├── hooks.go                     # PostRunHook 链 + Brain/KI Hook
│   │       ├── facades.go                   # ChatEngine/SessionManager/ModelManager/ToolAdmin
│   │       └── delta.go                     # DeltaSink (SSE 流)
│   │
│   ├── infrastructure/
│   │   ├── config/                          # 配置
│   │   │   ├── config.go                    # Config struct + ServerConfig
│   │   │   ├── manager.go                   # fsnotify + 热更新 + env 展开
│   │   │   ├── defaults.go                  # 默认值
│   │   │   └── paths.go                     # 路径解析
│   │   ├── llm/                             # LLM 适配
│   │   │   ├── types.go                     # Request/Response/ToolDef
│   │   │   ├── router.go                    # 多 Provider 路由 + 降级链 + Reload
│   │   │   ├── policy.go                    # 模型策略表
│   │   │   └── openai/client.go             # OpenAI 兼容客户端
│   │   ├── prompt/                          # 提示词引擎
│   │   │   ├── engine.go                    # 15-Section 组装 + 4 级裁剪
│   │   │   ├── discovery.go                 # 3 层文件发现
│   │   │   └── prompttext/
│   │   │       ├── prompttext.go            # 常量: 提示词 + 临时消息
│   │   │       └── template.go              # Render + Wrap
│   │   ├── sandbox/sandbox.go               # 子进程管理 + SendInput + SIGTERM→SIGKILL
│   │   ├── security/hook.go                 # 6 方法 AgentHook + 审批 + 注入检测
│   │   ├── persistence/                     # SQLite + GORM
│   │   │   ├── db.go                        # 数据库连接
│   │   │   ├── repository.go                # 3 实体 CRUD
│   │   │   └── history.go                   # 对话历史持久化
│   │   ├── brain/store.go                   # Artifact + Checkpoint + Meta + Snapshot
│   │   ├── knowledge/store.go               # KI CRUD + 搜索 + 摘要
│   │   ├── skill/manager.go                 # SKILL.md 发现 + Forge + HasCommand + Watcher
│   │   ├── workspace/store.go               # .ngoagent/ + HeartbeatState + Analyze
│   │   ├── forge/                           # 锻造引擎
│   │   │   ├── engine.go                    # Session 编排 + 重试循环
│   │   │   ├── sandbox.go                   # 沙箱创建/清理
│   │   │   ├── assertion.go                 # 3 类断言
│   │   │   ├── diagnosis.go                 # 4 类失败诊断
│   │   │   └── tracker.go                   # JSON 历史 + 成功率
│   │   ├── mcp/manager.go                   # JSON-RPC 2.0 over stdio
│   │   ├── heartbeat/engine.go              # 定期后台任务执行
│   │   └── tool/                            # 14 个内建工具
│   │       ├── registry.go                  # 注册 + Enable/Disable + RWMutex
│   │       ├── filestate.go                 # 编辑安全 (SHA256)
│   │       └── (14 tool files)              # read/write/edit/glob/grep/cmd/web/forge...
│   │
│   ├── application/builder.go               # 8 阶段依赖注入 + 热更新 + Shutdown
│   └── interfaces/server/
│       ├── server.go                        # HTTP/SSE + Slash 命令
│       └── api.go                           # REST API (/session /tools /context /security)
│
└── pkg/ctxutil/ctxutil.go                   # 跨层 Context 工具
```
