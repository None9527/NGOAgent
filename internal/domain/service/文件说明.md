# domain/service 文件说明

> AgentLoop 核心引擎及其拆分后的模块文件清单。

## 主循环

| 文件 | 行数 | 职责 |
|------|------|------|
| `run.go` | 674 | ReAct 主循环 (`Run`, `runInner`, `doPrepare`, `doGenerate`) |
| `run_helpers.go` | 287 | 运行时信息构建、Git 快照、Token 估算、工具分级加载 |
| `evo_controller.go` | 187 | Evo 评估调度 (`fireHooks`, `runEvoEval`, `pushEvo`) |
| `persistence_ops.go` | 107 | 历史增量/全量持久化、子代理转录保存 |

## 核心组件

| 文件 | 行数 | 职责 |
|------|------|------|
| `loop.go` | 444 | `AgentLoop` 结构体定义、`Deps` 依赖注入、生命周期管理 |
| `state.go` | — | 10 态状态机定义和合法转换表 |
| `guard.go` | 354 | `BehaviorGuard` — 行为防护 (步数限制、重复检测、空响应检测) |
| `barrier.go` | 294 | `SubagentBarrier` — 并行子代理协调 (P0-3: finalized flag) |
| `compact.go` | 565 | 3 级 Context 压缩 (tool-heavy → LLM summary → force truncate) |
| `prepare.go` | 280 | `doPrepare` — Ephemeral 注入、workspace context、KI、附件 |

## 安全与工具

| 文件 | 行数 | 职责 |
|------|------|------|
| `security_middleware.go` | 97 | `checkSecurity` — 统一安全决策入口 (AccessLevel fast path) |
| `tool_exec.go` | 570 | 工具执行 (`doToolExec`, 并发/串行分流, 输出分级处理) |
| `protocol.go` | — | 工具协议信号分发 (`Dispatch`) |

## 扩展功能

| 文件 | 行数 | 职责 |
|------|------|------|
| `hooks.go` | 471 | 生命周期钩子 (title distill, KI distill, memory, webhook) |
| `facades.go` | 329 | 公共 API 门面 (ClearHistory, Compact, Retry, StripLastTurn) |
| `buffered_delta.go` | 326 | 带缓冲的 SSE/WS 事件写入器 (重连回放) |
| `loop_pool.go` | 277 | `LoopPool` — 会话级 AgentLoop 实例池 (LRU 驱逐) |
| `evo_evaluator.go` | 276 | Evo LLM 评估器 (prompt 构建 + JSON 解析) |
| `evo_repair.go` | — | Evo 修复路由 (策略选择 + 断路器) |
| `phase_dream.go` | 264 | 执行阶段检测 (planning → coding → testing) |

## 测试

| 文件 | 职责 |
|------|------|
| `guard_test.go` | Guard 行为规则测试 (12 tests) |
| `core_test.go` | Config 零值、ToolMeta、Barrier 并发、Guard 线程安全 (7 tests) |
| `p0a_core_test.go` | P0 安全修复验证测试 |
